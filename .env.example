export POSTGRES_USER=postgres
export POSTGRES_PASSWORD=topsecret
export POSTGRES_DB=wikipediagolf-dev-db
export DB_HOST=localhost
export DB_PORT=5432

export DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}?sslmode=disable

export FIREBASE_CONFIG=service-account.json 

# For development environment only
export FIREBASE_AUTH_EMULATOR_HOST="127.0.0.1:9099"

